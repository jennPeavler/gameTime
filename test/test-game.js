var assert = require('chai').assert
var Game = require('../lib/game')

//describe game

//it should be a function

//it should be an object

//it should have a width

//it should have a height

//it should be able to detect the ball with the left boundary

//it should be able to detect the ball with the right boundary

//it should be able to detect the ball with the top boundary

//it should be able to detect the ball with the bottom boundary

//brick should be removed after hit by ball
//maybe use score increment to test this??

//life should decrement if ball touches bottom boundary

//
